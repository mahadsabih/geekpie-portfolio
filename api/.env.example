# SMTP Configuration for Contact Form
# Copy this file to .env and fill in your values
# Or set these as environment variables in Vercel dashboard

# SMTP Server Settings
# For Gmail: smtp.gmail.com, port 587, secure=false
# For Outlook: smtp.office365.com, port 587, secure=false
# For custom SMTP: check your email provider's settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# SMTP Authentication
# For Gmail, you need to use an App Password:
# 1. Go to your Google Account > Security
# 2. Enable 2-Step Verification if not already enabled
# 3. Go to App Passwords and create a new one
# 4. Use that App Password here (not your regular password)
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here

# Contact Email (where form submissions will be sent)
# If not set, defaults to SMTP_USER
CONTACT_EMAIL=contact@geekpie.com
